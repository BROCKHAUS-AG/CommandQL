namespace BROCKHAUSAG {
    export enum ConnectionStatus { None, Connected, Disconnected, Poll }

    export class HttpHelper {

        public makeRequest(): void {
            return;
        }
    }
}